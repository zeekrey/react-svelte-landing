import { styled } from "../stitches.config";

const Input = styled("input", {
  all: "unset",
  boxSizing: "border-box",
  padding: 10,
  borderRadius: 4,
  background: "$green12",
  color: "$green4",
});

export default Input;
